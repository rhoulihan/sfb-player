# Copyrighted source material — owner's SFB PDFs (huge, do not commit)
SFB/

# Tooling / test artifacts
.playwright-mcp/
node_modules/

# Stray screenshots at repo root (from Playwright verification runs)
/*.png

# Regenerable intermediate SSD render (keep image.png; raw is pre-rotation)
ssd-pipeline/data/*/raw.png

# Python / OS / editor cruft
__pycache__/
*.pyc
.DS_Store
Thumbs.db
*.swp

# Copyrighted SSD page images — regenerate locally via ssd-pipeline/extract_images.py
ssd-pipeline/data/*/image.png
docs/spec/wireframes/assets/

# OCR title-strip cache for the SSD search (regenerable)
ssd-pipeline/data/_title_index.json

# weapon-table source crops (from copyrighted SSD/rulebook)
ssd-pipeline/data/_weapon-tables/

# weapon-table validation editor — local-only tool (kept for adding future races)
ssd-pipeline/viewer/weapons.html

# battle-state save file (local)
ssd-pipeline/data/_battle.json

# race console art (large, provided locally like SSD images)
assets/*.png

ssd-pipeline/viewer/assets/*.png
